#!/usr/bin/env python3
# -*- coding: utf-8 -*-
# data_class.py


class Data:
    def __init__(self, timestamp,
                 coordinates, date,
                 duration, weather,
                 cluster_rate, alpha_rate,
                 beta_rate, gamma_rate,
                 surrounding_geology, dose,
                 other_comments, deviation_from_method)
        self._timestamp = timestamp
        self._coordinates = coordinates
        self._date = date
        self._duration = duration
        self._weather = weather
        self._cluster_rate = cluster_rate
        self._alpha_rate = alpha_rate
        self._beta_rate = beta_rate
        self._gamma_rate = gamma_rate
        self._surrounding_geology = surrounding_geology
        self._dose = dose
        self._other_comments = other_comments
        self._deviation_from_method = deviation_from_method


#"timestamp"
#"coordinates"
#"date"
#"duration"
#"weather"
#"cluster_rate"
#"alpha_rate"
#"beta_rate"
#"gamma_rate"
#"surrounding_geology"
#"dose"
#"other_comments"
#"deviation_from_method"
